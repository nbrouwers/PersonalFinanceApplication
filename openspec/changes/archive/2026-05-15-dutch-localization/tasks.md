## 1. Formatting Utility

- [x] 1.1 Create `frontend/src/utils/format.js` with `formatEuro` and `formatDate` functions using `Intl.NumberFormat('nl-NL')` and `Intl.DateTimeFormat('nl-NL')`

## 2. Dashboard Page

- [x] 2.1 Apply `formatEuro` to total balance, account pie chart tooltips, budget progress amounts, goal target/current amounts in DashboardPage
- [x] 2.2 Apply `formatDate` to goal target date display in DashboardPage

## 3. Accounts Page

- [x] 3.1 Apply `formatEuro` to balance column in AccountList
- [x] 3.2 Apply `formatEuro` to success/confirmation messages in AccountForm and AccountsPage (no amounts in messages, no change needed)

## 4. Budgets Page

- [x] 4.1 Apply `formatEuro` to budget amounts in BudgetAlert and BudgetForm
- [x] 4.2 Apply `formatDate` to budget start/end dates in BudgetForm confirmation and BudgetsPage

## 5. Goals Page

- [x] 5.1 Apply `formatEuro` to goal target amounts in GoalForm, GoalsPage
- [x] 5.2 Apply `formatDate` to goal target dates in GoalForm, GoalsPage

## 6. CSV Import Page

- [x] 6.1 Apply `formatEuro` to import result amounts in CsvUpload
- [x] 6.2 Apply `formatDate` to transaction dates in import results

## 7. Ubiquitous Language

- [x] 7.1 Update `openspec/ubiquitous-language.md` to include Dutch locale formatting terms