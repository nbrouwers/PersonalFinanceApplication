## Context

Current import preview shows generic fields. Triodos CSV has specific columns that users recognize.

## Goals / Non-Goals

**Goals:**
- Show Date, IBAN, Amount, Credit/Debit, Name, Description in preview
- Map IBAN to account names when available

**Non-Goals:**
- Editable preview (read-only is fine)
- Transaction kind column